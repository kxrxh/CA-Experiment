.text
    add r1, r0, #1 // a
    add r2, r0, #2 // b

    add r4, r0, #10 // N

loop:
    beq r4, r0, exit // if (N == 0) -> done

    // print a

    add r3, r1, r2 // sum = a + b
    add r1, r0, r2 // a = b
    add r2, r0, r3 // b = sum

    sub r4, r4, #1

    jmp loop

exit: 
    halt